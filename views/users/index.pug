h1 Users Site
form(action="/users/search",method="GET",title="Type name")
    input(type="text",name='q')
    button Search
ul 
    each user in users
        li
            span=user.name
            span &nbsp;
            a(href="/users/" + user.id) View 